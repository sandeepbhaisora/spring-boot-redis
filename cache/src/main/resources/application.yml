spring:
  application:
    name: cache
  redis:
    host: localhost
    port: 6379
  cache:
    redis:
      cache-null-values: false
      time-to-live: 10s
server:
  port: 8766
  servlet:
    context-path: /cache